export const url = 'http://localhost:8000';
export const signinUrl = `${url}/api/auth/signin`;
export const getUserDataUrl = `${url}/api/test/admin`;
export const refreshTokenUrl = `${url}/api/auth/refreshToken`;
